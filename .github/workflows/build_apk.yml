name: Build VigiRoutes Client APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.32.0'
          channel: stable
          cache: true

      - name: Create local.properties
        run: |
          echo "sdk.dir=$ANDROID_HOME" > android/local.properties
          echo "MAPS_API_KEY=${{ secrets.MAPS_API_KEY }}" >> android/local.properties

      - name: Decode keystore
        run: echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 --decode > android/app/vigiroutes-release.jks

      - name: Create key.properties
        run: |
          echo "storePassword=${{ secrets.STORE_PASSWORD }}" > android/key.properties
          echo "keyPassword=${{ secrets.KEY_PASSWORD }}"     >> android/key.properties
          echo "keyAlias=vigiroutes"                         >> android/key.properties
          echo "storeFile=app/vigiroutes-release.jks"        >> android/key.properties

      - name: Flutter pub get
        run: flutter pub get

      - name: Show all plugins
        run: flutter pub deps --style=list | head -50

      - name: Check GeneratedPluginRegistrant
        run: |
          find . -name "GeneratedPluginRegistrant.java" -o -name "GeneratedPluginRegistrant.kt" 2>/dev/null | head -5
          cat android/app/src/main/java/io/flutter/plugins/GeneratedPluginRegistrant.java 2>/dev/null || echo "File not found"

      - name: Build APK Release
        run: flutter build apk --release
        env:
          MAPS_API_KEY: ${{ secrets.MAPS_API_KEY }}

      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: vigiroutes-client-${{ github.run_number }}
          path: build/app/outputs/flutter-apk/app-release.apk
          retention-days: 30
